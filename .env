PORT=3001
JWT_SECRET=vendor-platform-jwt-secret-change-in-production-2026
JWT_EXPIRES_IN=8h
DB_PATH=./server/data/vendor_platform.db
UPLOADS_DIR=./server/uploads

# Email (leave blank to use Ethereal test account in dev)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@onction.com

# Set to 'production' to enforce stricter auth
NODE_ENV=development
